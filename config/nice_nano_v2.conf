# ZMK 配置文件
# 启用 OLED 显示
CONFIG_ZMK_DISPLAY=y

# 启用 GPIO 直键扫描驱动
CONFIG_ZMK_KSCAN_GPIO_DIRECT=y

# 启用基本行为
CONFIG_ZMK_BEHAVIORS=y

# 启用键盘映射
CONFIG_ZMK_KEYMAP=y

# 启用层切换
CONFIG_ZMK_LAYER=y

# 启用 OLED 驱动
CONFIG_DISPLAY=y
CONFIG_SSD1306=y
